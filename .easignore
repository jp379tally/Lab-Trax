# Node modules — EAS installs these fresh on its build servers
node_modules/
.pnpm-store/

# Git history
.git/

# Build outputs
dist/
build/
static-build/
server_dist/
.expo/
*.tsbuildinfo

# Desktop app artifact — Electron + native binaries (566 MB), not needed for mobile build
artifacts/labtrax-desktop/

# API server artifact — not needed for mobile build
artifacts/api-server/

# Mockup sandbox — not needed for mobile build
artifacts/mockup-sandbox/

# Large assets not needed for mobile build
attached_assets/

# Replit-internal / cache directories
.cache/
.upm/
.agents/
.config/
.migration-backup/
.pythonlibs/

# Binaries and archives
*.exe
*.zip
*.tar.gz
*.tar.xz
*.dmg
*.pkg

# Logs and temp files
*.log
logs/
tmp/
.local/
