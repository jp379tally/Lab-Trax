I extracted and reviewed the newest code from `E:\Lab Software\Code\Lab-Trax.zip`. I found several real issues that line up with what you’re seeing: desktop can list SDR1 cases, mobile can show zero, and desktop case clicks don’t reliably open usable case tools.

::code-comment{title="[P1] Mobile app can point at a different backend than desktop" body="The native mobile app falls back to the hardcoded URL `https://lab-trax.replit.app/` when `EXPO_PUBLIC_DOMAIN` is not present, while the desktop app uses the current site origin via relative `/api` calls. If the App Store build is pointed at an older or empty backend, mobile will correctly show zero cases even while desktop shows the current database. Replit should remove the hardcoded fallback risk and make mobile/desktop use the exact same production API base URL." file="E:/Lab Software/Code/Lab-Trax-review-2026-05-07/Lab-Trax/artifacts/labtrax/lib/query-client.ts" start=8 end=34 priority=1 confidence=0.95}

::code-comment{title="[P1] Desktop lists legacy mobile cases but opens them through the wrong case system" body="The server bridges legacy `lab_cases` rows into the desktop list with `_source: 'mobile'`, but clicking any row always opens `CaseDrawer`, which fetches `/cases/:id`. That route only looks in the structured `cases` table, so bridged mobile cases can appear in the desktop table but fail to open/edit/invoice/attach because the detail endpoint cannot find them. Replit needs either one unified case table/API or a real legacy-case detail/action bridge." file="E:/Lab Software/Code/Lab-Trax-review-2026-05-07/Lab-Trax/artifacts/labtrax-desktop/src/pages/cases.tsx" start=756 end=930 priority=1 confidence=0.95}

::code-comment{title="[P1] Two case tables are causing platform split-brain" body="Mobile writes cases to `/api/legacy/cases` backed by `lab_cases`, while desktop creates and edits structured cases through `/api/cases` backed by `cases`. The bridge only converts rows for display; it does not keep both tables synchronized. A case can therefore exist or be updated in one system while the other platform sees stale, missing, or non-interactive data. This should be consolidated so SDR1 cases have one canonical backend record and both platforms use the same endpoints." file="E:/Lab Software/Code/Lab-Trax-review-2026-05-07/Lab-Trax/artifacts/labtrax/lib/app-context.tsx" start=413 end=451 priority=1 confidence=0.9}

::code-comment{title="[P2] Wrong or empty server response can wipe mobile’s local lab case cache" body="The mobile reconciliation treats an OK server response as authoritative and drops any locally cached lab-tagged case that the server did not return. That is reasonable only if the app is guaranteed to be talking to the correct backend with correct memberships. Combined with the hardcoded fallback URL, an empty response from the wrong backend can make SDR1 appear empty on mobile." file="E:/Lab Software/Code/Lab-Trax-review-2026-05-07/Lab-Trax/artifacts/labtrax/lib/case-reconciliation.ts" start=14 end=46 priority=2 confidence=0.85}

**Main Takeaway**
The desktop HUD/interface may have changed, but the data model is still split. Replit needs to stop treating desktop and mobile as separate case systems. The fix should be: one production API URL, one canonical case source of truth, and one permissions layer used by desktop and mobile.

I also checked syntax on the built API bundle with Node and it passed. I could not run the full TypeScript/PNPM build here because this environment has Node available but not `pnpm`/`npm` on PATH.