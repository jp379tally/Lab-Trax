The latest changes broke the iPhone UI badly. This is not a minor spacing issue. The screen container/layout structure is wrong and must be audited systematically.

What I need you to do:
Do a root-cause layout audit of the affected mobile screens instead of making superficial styling tweaks.

Visible regressions in TestFlight:
1. Dashboard/profile screen shows a huge empty dashed container taking up most of the screen.
2. Content that used to be laid out normally is now compressed into the top area.
3. File Drop Zone screen is trapped inside a narrow centered column with massive blank space.
4. Bottom nav renders, so the app loads, but the screen composition is clearly broken.
5. iPhone layout now looks like tablet/desktop styles are being incorrectly applied.

I want you to inspect and compare the last working version vs. the current broken version for these exact screens/components:
- Dashboard screen
- Profile/admin screen
- File Drop Zone / upload screen
- Any shared Screen/Page/Layout wrapper
- Any shared Card/Container component
- Any SafeAreaView / ScrollView wrapper
- Any responsive helper or breakpoint utility
- Any recent global style changes affecting width/flex alignment

Audit these properties first in parent wrappers, not just inner components:
- flex
- flexGrow
- flexShrink
- width
- minWidth
- maxWidth
- height
- minHeight
- maxHeight
- alignItems
- alignSelf
- justifyContent
- padding
- paddingHorizontal
- margin
- marginHorizontal
- position
- inset/top/bottom/left/right
- overflow
- contentContainerStyle
- gap / rowGap / columnGap

Very likely root causes:
- A tablet/iPad responsive layout was accidentally applied to iPhone
- A shared page wrapper now uses maxWidth or centered column behavior on phone
- A dashed upload-box style was promoted to a parent screen wrapper
- A ScrollView contentContainerStyle is forcing oversized vertical space
- A flex/layout rule changed globally and is collapsing screen composition
- Safe area or container nesting was changed and now constrains content width/height

Do this in order:
1. Identify the exact parent container where the phone layout starts to go wrong.
2. Trace layout from the outermost screen wrapper inward.
3. Compare current code against the last known good mobile layout.
4. Revert any broad responsive/global layout changes that caused this regression.
5. Separate iPhone and iPad behavior explicitly with breakpoints instead of shared assumptions.
6. Make iPhone screens use full-width mobile layout again with normal padding.
7. Ensure the dashed border only wraps the upload zone component itself, not the main page structure.
8. Verify the dashboard header/profile area sits in a normal vertical flow, not inside an oversized constrained wrapper.

Important:
- Do not “tweak until it looks better.”
- Do not just reduce margins or padding.
- Find the parent layout component causing the regression and fix that source.
- Preserve iPad improvements, but only behind proper device-specific layout logic.

Testing requirements before republishing:
- Test on iPhone-size viewport
- Test on iPad viewport separately
- Confirm dashboard screen looks normal on phone
- Confirm upload/drop zone screen fills the screen correctly on phone
- Confirm no giant dashed empty box appears unless it is the actual drop zone
- Confirm content is not trapped in a narrow centered column on iPhone

Deliverables I want back:
1. The exact file(s) causing the regression
2. The exact root cause
3. What was reverted or changed
4. Confirmation that both iPhone and iPad layouts were tested before the next build