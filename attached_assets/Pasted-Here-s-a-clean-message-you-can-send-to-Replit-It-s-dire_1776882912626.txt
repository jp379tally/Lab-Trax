Here’s a clean message you can send to Replit. It’s direct, technical, and sets clear acceptance criteria so nothing gets half-done:

---

## 🚨 LabTrax Backup & Data Integrity – Required Fixes

We recently experienced complete case loss across users. While the deletion bug appears to have been addressed, the system is still **not production-safe** because there is no reliable backup and restore architecture in place.

I’ve implemented a backend patch that introduces proper backup and restore functionality. I need you to review, integrate, and confirm the following requirements are met.

---

## 📦 Patch to Review

A backend patch has been prepared that includes:

* Full system backup endpoint
* Restore endpoint with safety checks
* Daily automated backup scheduler (server-side)
* OneDrive upload integration (Microsoft Graph)
* Local backup storage
* Audit logging for backup/restore

Please integrate this patch into the current backend and confirm compatibility.

---

## ✅ REQUIRED ACCEPTANCE CRITERIA

### 1. Full Database Backup (MANDATORY)

* Endpoint: `POST /api/backups/full`
* Must include:

  * Cases
  * Invoices
  * Doctors
  * Organizations
  * Users
  * Memberships
  * Attachments (at least metadata)
  * Audit logs
* Must run **entirely server-side** (no frontend dependency)

---

### 2. Restore System (CRITICAL)

* Endpoint: `POST /api/backups/restore`
* Must:

  * Accept backup file
  * Support:

    * `replace` (wipe + restore)
    * `merge` (safe import)
  * Require confirmation string (`"RESTORE"`)
  * Fully rebuild system state

---

### 3. True Daily Automated Backup (NOT app-triggered)

* Must run via **server scheduler / cron**
* Must NOT depend on:

  * User login
  * App being open
* Target: once every 24 hours (e.g., 2 AM UTC)

---

### 4. Real OneDrive Integration (NOT local folder)

Current behavior appears to write to a local directory labeled as OneDrive.

We need:

* Microsoft Graph API integration
* OAuth token handling (client credentials or refresh flow)
* Verified upload to actual OneDrive account

**Deliverable proof:**

* A backup file visible in OneDrive
* Timestamped and accessible outside the app

---

### 5. Multi-location Backup (Redundancy)

Backups must exist in:

* OneDrive (cloud)
* Local server directory
* Downloadable file (for USB/manual storage)

---

### 6. Admin UI Integration

Update Settings → Data section:

* “Export Cases” → **Create LabTrax Backup**
* Add: **Import LabTrax Data**

These must:

* Call real backend endpoints
* Not rely on AsyncStorage
* Trigger full system backup/restore

---

### 7. Restore Test (MANDATORY PROOF)

Please complete and confirm:

1. Create test data (cases, invoices, etc.)
2. Run backup
3. Delete all data
4. Run restore
5. Verify everything returns correctly

If this cannot be demonstrated, the system is not production ready.

---

### 8. Data Safety Rules

* NEVER auto-delete cases on:

  * network failure
  * server restart
  * temporary auth issues
* Deletions must be:

  * explicit user action
  * logged via audit system

---

## ⚠️ Current Risk Status

Without these changes:

* Data loss is still possible
* Backups are not guaranteed
* Recovery is not reliable

---

## 🎯 Final Deliverables Required

Please confirm with evidence:

* ✅ Backup endpoint working
* ✅ Restore endpoint working
* ✅ Daily automated backup running
* ✅ Real OneDrive upload verified
* ✅ Restore test completed successfully

---

If anything in the patch needs adjustment for your environment, that’s fine — but the **functional requirements above are not optional**.

---

If you want, I can also:

* Review their response when they send it back
* Or verify their implementation step-by-step before you trust it again

Just send it over 👍
