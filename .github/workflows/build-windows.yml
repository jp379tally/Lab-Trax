name: Build Windows Installer (Test)

# Standalone unsigned build for local testing and CI smoke checks.
# For official versioned releases, use the Release workflow (.github/workflows/release.yml),
# which runs both platform builds in parallel and publishes a single unified GitHub Release.

on:
  workflow_dispatch:
    inputs:
      api_base_url:
        description: "Production API base URL (e.g. https://your-app.replit.app)"
        required: true
        type: string

jobs:
  build:
    runs-on: windows-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          # BUILD_BOT_TOKEN should be a fine-grained PAT (or GitHub App token)
          # with "bypass branch protection" rights so the build-number commit-back
          # push succeeds even when main requires a PR or status checks.
          # Falls back to the default GITHUB_TOKEN on repos with no branch
          # protection — callers that have not set the secret get the old behaviour.
          token: ${{ secrets.BUILD_BOT_TOKEN || github.token }}

      - uses: pnpm/action-setup@v4
        with:
          version: 10.26.1

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build and package Windows installer
        # Signing is optional for test builds — omitting CSC_LINK produces an
        # unsigned installer. No GH_TOKEN is passed so no release is created.
        run: pnpm --filter @workspace/labtrax-desktop run electron:build
        env:
          VITE_API_BASE_URL: ${{ github.event.inputs.api_base_url }}
          CSC_LINK: ${{ secrets.WINDOWS_CERTIFICATE }}
          CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}

      # Persist the incremented build-number.json so that the counter stays
      # monotonically increasing across runs. Without this commit the file
      # resets on the next clean checkout and two builds can share a number.
      # [skip ci] prevents this maintenance commit from re-triggering CI.
      - name: Commit bumped build number
        id: commit-build-number
        if: success()
        shell: bash
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add artifacts/labtrax-desktop/build-number.json
          if git diff --cached --quiet; then
            echo "build-number.json unchanged — nothing to commit."
            exit 0
          fi
          BUILD_NUM=$(node -p "require('./artifacts/labtrax-desktop/build-number.json').buildNumber")
          git commit -m "chore: bump desktop build number to ${BUILD_NUM} [skip ci]"
          # Retry with rebase in case a parallel job pushed first.
          # If the rebase ends with nothing new to push (both jobs wrote the
          # same N+1), we mark success without an extra push.
          pushed=false
          for attempt in 1 2 3; do
            if git pull --rebase origin "$GITHUB_REF_NAME" \
                && git push origin "HEAD:${GITHUB_REF_NAME}"; then
              pushed=true
              break
            fi
            echo "::warning::Push attempt ${attempt} failed — retrying in ${attempt}s…"
            sleep "$attempt"
          done
          if [[ "$pushed" != "true" ]]; then
            echo "push_failed=true" >> "$GITHUB_OUTPUT"
            echo "::warning::All push attempts failed — build number was NOT persisted. The build artifact is still usable; download the build-counter-fallback workflow artifact and apply it manually (see docs/build-counter-recovery.md)."
          fi

      - name: Upload build counter fallback artifact
        if: always() && steps.commit-build-number.outputs.push_failed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: build-counter-fallback
          path: artifacts/labtrax-desktop/build-number.json

      # Alert admins when the build-number commit fails to push so the silent
      # counter-reset bug is caught immediately rather than discovered later.
      # Uses the same opt-in gating as the publish-failure alert: both secrets
      # must be set, otherwise the step skips without error.
      - name: Notify admins on build-number push failure
        if: always() && steps.commit-build-number.outputs.push_failed == 'true'
        shell: bash
        env:
          PLATFORM_ADMIN_SECRET: ${{ secrets.PLATFORM_ADMIN_SECRET }}
          PUBLISH_API_BASE_URL: ${{ secrets.PUBLISH_API_BASE_URL }}
        run: |
          if [[ -z "$PLATFORM_ADMIN_SECRET" || -z "$PUBLISH_API_BASE_URL" ]]; then
            echo "::notice::Skipping build-number push failure alert — PLATFORM_ADMIN_SECRET or PUBLISH_API_BASE_URL not set."
            exit 0
          fi
          BASE_URL="${PUBLISH_API_BASE_URL%/}"
          RUN_URL="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          BUILD_NUM_FOR_ALERT=$(node -p "require('./artifacts/labtrax-desktop/build-number.json').buildNumber" 2>/dev/null || echo "")
          PAYLOAD=$(jq -n \
            --arg workflowName "${GITHUB_WORKFLOW}" \
            --arg runUrl "$RUN_URL" \
            --arg runId "${GITHUB_RUN_ID}" \
            --arg commitSha "${GITHUB_SHA}" \
            --arg ref "${GITHUB_REF_NAME}" \
            --arg stage "build-number-commit" \
            --arg errorMessage "All 3 push attempts to persist build-number.json to ${GITHUB_REF_NAME} failed. The build number counter may reset on the next checkout — check branch protection rules or network connectivity." \
            --argjson attemptedBuildNumber "${BUILD_NUM_FOR_ALERT:-null}" \
            '{workflowName:$workflowName, runUrl:$runUrl, runId:$runId, commitSha:$commitSha, ref:$ref, stage:$stage, errorMessage:$errorMessage, attemptedBuildNumber:$attemptedBuildNumber}')
          echo "Reporting build-number push failure to $BASE_URL/api/admin/desktop-installer/publish-failure"
          NOTIFY_BODY=$(mktemp)
          NOTIFY_STATUS=$(curl --silent --show-error --output "$NOTIFY_BODY" --write-out '%{http_code}' \
            -X POST \
            -H "X-Platform-Admin-Secret: $PLATFORM_ADMIN_SECRET" \
            -H "Content-Type: application/json" \
            --data "$PAYLOAD" \
            "$BASE_URL/api/admin/desktop-installer/publish-failure") || true
          NOTIFY_STATUS="${NOTIFY_STATUS:-000}"
          cat "$NOTIFY_BODY"; echo
          if [[ ! "$NOTIFY_STATUS" =~ ^2[0-9][0-9]$ ]]; then
            BODY_TRUNC=$(head -c 500 "$NOTIFY_BODY")
            echo "::warning::Failed to deliver build-number push failure alert (HTTP $NOTIFY_STATUS): $BODY_TRUNC — admins will not receive an email for this run."
          fi

      - name: Verify installer signature
        if: env.CSC_LINK != ''
        shell: pwsh
        env:
          CSC_LINK: ${{ secrets.WINDOWS_CERTIFICATE }}
        run: |
          $installers = Get-ChildItem -Path "artifacts/labtrax-desktop/electron-dist" -Filter "*.exe" -ErrorAction Stop
          if ($installers.Count -eq 0) {
            Write-Host "::error::No .exe installer found in artifacts/labtrax-desktop/electron-dist."
            exit 1
          }
          $failed = $false
          foreach ($installer in $installers) {
            Write-Host "Verifying signature: $($installer.Name)"
            signtool verify /pa /v "$($installer.FullName)"
            if ($LASTEXITCODE -ne 0) {
              Write-Host "::error::Signature verification FAILED for $($installer.Name). The certificate may be misconfigured or expired, or electron-builder did not sign the output. Check that WINDOWS_CERTIFICATE and WINDOWS_CERTIFICATE_PASSWORD are valid and that the CSC_LINK env var was passed to the build step."
              $failed = $true
            }
          }
          if ($failed) { exit 1 }
          Write-Host "All installers passed signature verification."

      - name: Upload installer as workflow artifact
        # electron-builder is configured (electron-builder.yml → nsis.artifactName)
        # to produce a stable filename: LabTrax-Setup.exe. Admins download this
        # zip from the GitHub Actions run summary and then upload the contained
        # LabTrax-Setup.exe through Settings → Desktop App.
        uses: actions/upload-artifact@v4
        with:
          name: LabTrax-Windows-Installer
          path: artifacts/labtrax-desktop/electron-dist/LabTrax-Setup.exe
          if-no-files-found: error

      - name: Upload portable ZIP as workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: LabTrax-Windows-Portable
          path: artifacts/labtrax-desktop/electron-dist/LabTrax-Windows-Portable.zip
          if-no-files-found: warn

      # Auto-publish the freshly built installer to the live download page so
      # admins don't have to download → upload → update URL by hand. Gated by
      # the PLATFORM_ADMIN_SECRET and PUBLISH_API_BASE_URL secrets — if either
      # is unset, this step logs a notice and exits 0 (safe to disable).
      - name: Publish installer to live download page
        id: publish
        if: success()
        shell: bash
        env:
          PLATFORM_ADMIN_SECRET: ${{ secrets.PLATFORM_ADMIN_SECRET }}
          PUBLISH_API_BASE_URL: ${{ secrets.PUBLISH_API_BASE_URL }}
        run: |
          set -uo pipefail
          if [[ -z "$PLATFORM_ADMIN_SECRET" || -z "$PUBLISH_API_BASE_URL" ]]; then
            echo "::notice::Skipping auto-publish — PLATFORM_ADMIN_SECRET or PUBLISH_API_BASE_URL secret not set."
            exit 0
          fi
          BASE_URL="${PUBLISH_API_BASE_URL%/}"
          INSTALLER_PATH="artifacts/labtrax-desktop/electron-dist/LabTrax-Setup.exe"
          # Expose context for the on-failure notify step. These are written to
          # $GITHUB_OUTPUT so the failure step can attach them to the alert
          # email even if we error out before computing $VERSION below.
          echo "base_url=$BASE_URL" >> "$GITHUB_OUTPUT"
          echo "stage=unknown" >> "$GITHUB_OUTPUT"
          if [[ ! -f "$INSTALLER_PATH" ]]; then
            echo "stage=upload" >> "$GITHUB_OUTPUT"
            echo "error_message=Installer not found at $INSTALLER_PATH" >> "$GITHUB_OUTPUT"
            echo "::error::Installer not found at $INSTALLER_PATH; cannot publish."
            exit 1
          fi
          VERSION=$(node -p "require('./artifacts/labtrax-desktop/package.json').version")
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          # Pull release notes for this version out of RELEASE_NOTES.md if
          # present, falling back to a generic CI message. Same logic as
          # before — just now sent inline to /publish.
          NOTES=$(node -e "
const fs = require('fs');
const ver = 'v${VERSION}';
let raw;
try { raw = fs.readFileSync('artifacts/labtrax-desktop/RELEASE_NOTES.md', 'utf8'); } catch { process.exit(0); }
const lines = raw.split(/\r?\n/);
let inBlock = false, out = [];
for (const line of lines) {
  if (/^## v/.test(line)) {
    if (inBlock) break;
    if (line.replace(/^##\s+/, '').trim() === ver) inBlock = true;
    continue;
  }
  if (inBlock) out.push(line);
}
process.stdout.write(out.join('\n').trim());
" 2>/dev/null) || true
          if [[ -z "$NOTES" ]]; then
            NOTES="Auto-published from GitHub Actions run ${GITHUB_RUN_ID} (commit ${GITHUB_SHA})."
            echo "::notice::No entry for v${VERSION} found in RELEASE_NOTES.md — using generic notes."
          else
            echo "::notice::Using release notes for v${VERSION} from RELEASE_NOTES.md."
          fi
          RUN_URL="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          # Atomic publish (Task #749): single multipart call to /publish
          # uploads the installer AND updates system_settings + changelog in
          # one DB write. Previously this was three separate curls (upload,
          # settings PUT, on-failure notify); see
          # docs/desktop-publish-pipeline.md ("Why /publish exists") for the
          # rationale. The server dispatches a deduped admin alert if either
          # stage fails, so no follow-up notify step is needed.
          echo "Publishing $INSTALLER_PATH (v$VERSION) to $BASE_URL/api/admin/desktop-installer/publish"
          PUBLISH_BODY=$(mktemp)
          PUBLISH_STATUS=$(curl --silent --show-error --output "$PUBLISH_BODY" --write-out '%{http_code}' \
            -H "X-Platform-Admin-Secret: $PLATFORM_ADMIN_SECRET" \
            -F "file=@${INSTALLER_PATH};type=application/vnd.microsoft.portable-executable" \
            -F "version=${VERSION}" \
            -F "downloadUrl=/downloads/LabTrax-Setup.exe" \
            -F "releaseNotes=${NOTES}" \
            -F "workflowName=${GITHUB_WORKFLOW}" \
            -F "runUrl=${RUN_URL}" \
            -F "runId=${GITHUB_RUN_ID}" \
            -F "commitSha=${GITHUB_SHA}" \
            -F "ref=${GITHUB_REF_NAME}" \
            "$BASE_URL/api/admin/desktop-installer/publish") || true
          PUBLISH_STATUS="${PUBLISH_STATUS:-000}"
          cat "$PUBLISH_BODY"; echo
          if [[ ! "$PUBLISH_STATUS" =~ ^2[0-9][0-9]$ ]]; then
            BODY_TRUNC=$(head -c 1500 "$PUBLISH_BODY")
            STAGE_PARSED=$(jq -r '.stage // "unknown"' "$PUBLISH_BODY" 2>/dev/null || echo "unknown")
            echo "::error::Atomic publish failed (HTTP $PUBLISH_STATUS, stage=$STAGE_PARSED): $BODY_TRUNC"
            echo "::notice::Server-side dedup'd admin alert was dispatched by /publish — no follow-up notify needed."
            exit 1
          fi
          echo "::notice::Published LabTrax-Setup.exe v$VERSION to $BASE_URL (atomic /publish call)."

      # Publish the portable ZIP to the live download page so the
      # /downloads/LabTrax-Windows-Portable.zip URL works immediately
      # after CI completes. Same secret gating as the EXE step above.
      - name: Publish portable ZIP to live download page
        id: publish-zip
        if: success()
        shell: bash
        env:
          PLATFORM_ADMIN_SECRET: ${{ secrets.PLATFORM_ADMIN_SECRET }}
          PUBLISH_API_BASE_URL: ${{ secrets.PUBLISH_API_BASE_URL }}
        run: |
          set -uo pipefail
          if [[ -z "$PLATFORM_ADMIN_SECRET" || -z "$PUBLISH_API_BASE_URL" ]]; then
            echo "::notice::Skipping portable ZIP auto-publish — PLATFORM_ADMIN_SECRET or PUBLISH_API_BASE_URL secret not set."
            exit 0
          fi
          BASE_URL="${PUBLISH_API_BASE_URL%/}"
          ZIP_PATH="artifacts/labtrax-desktop/electron-dist/LabTrax-Windows-Portable.zip"
          if [[ ! -f "$ZIP_PATH" ]]; then
            echo "::notice::Portable ZIP not found at $ZIP_PATH — skipping ZIP publish (EXE was still published)."
            exit 0
          fi
          VERSION=$(node -p "require('./artifacts/labtrax-desktop/package.json').version")
          NOTES=$(node -e "
const fs = require('fs');
const ver = 'v${VERSION}';
let raw;
try { raw = fs.readFileSync('artifacts/labtrax-desktop/RELEASE_NOTES.md', 'utf8'); } catch { process.exit(0); }
const lines = raw.split(/\r?\n/);
let inBlock = false, out = [];
for (const line of lines) {
  if (/^## v/.test(line)) {
    if (inBlock) break;
    if (line.replace(/^##\s+/, '').trim() === ver) inBlock = true;
    continue;
  }
  if (inBlock) out.push(line);
}
process.stdout.write(out.join('\n').trim());
" 2>/dev/null) || true
          if [[ -z "$NOTES" ]]; then
            NOTES="Auto-published from GitHub Actions run ${GITHUB_RUN_ID} (commit ${GITHUB_SHA})."
          fi
          RUN_URL="${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/actions/runs/${GITHUB_RUN_ID}"
          echo "Publishing $ZIP_PATH (v$VERSION) to $BASE_URL/api/admin/desktop-installer/publish"
          PUBLISH_BODY=$(mktemp)
          PUBLISH_STATUS=$(curl --silent --show-error --output "$PUBLISH_BODY" --write-out '%{http_code}' \
            -H "X-Platform-Admin-Secret: $PLATFORM_ADMIN_SECRET" \
            -F "file=@${ZIP_PATH};type=application/zip" \
            -F "version=${VERSION}" \
            -F "downloadUrl=/downloads/LabTrax-Windows-Portable.zip" \
            -F "releaseNotes=${NOTES}" \
            -F "workflowName=${GITHUB_WORKFLOW}" \
            -F "runUrl=${RUN_URL}" \
            -F "runId=${GITHUB_RUN_ID}" \
            -F "commitSha=${GITHUB_SHA}" \
            -F "ref=${GITHUB_REF_NAME}" \
            "$BASE_URL/api/admin/desktop-installer/publish") || true
          PUBLISH_STATUS="${PUBLISH_STATUS:-000}"
          cat "$PUBLISH_BODY"; echo
          if [[ ! "$PUBLISH_STATUS" =~ ^2[0-9][0-9]$ ]]; then
            BODY_TRUNC=$(head -c 1500 "$PUBLISH_BODY")
            STAGE_PARSED=$(jq -r '.stage // "unknown"' "$PUBLISH_BODY" 2>/dev/null || echo "unknown")
            echo "::warning::Portable ZIP publish failed (HTTP $PUBLISH_STATUS, stage=$STAGE_PARSED): $BODY_TRUNC — EXE is still live."
          else
            echo "::notice::Published LabTrax-Windows-Portable.zip v$VERSION to $BASE_URL (atomic /publish call)."
          fi
